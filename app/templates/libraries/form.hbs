<h2 style="margin-bottom: 20px">{{title}}</h2>

<div class="row">
  <div class="col-md-6">
    <LibraryItemForm
        @item={{model}}
        @buttonLabel={{buttonLabel}}
        @action={{'saveLibrary'}} />
  </div>
  <div class="col-md-4 col-md-offset-1">
    <LibraryItem
        @item={{model}}>
      <br />    
    </LibraryItem>
  </div>
</div>